# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: GatewayService API
    version: 0.0.1
paths:
    /api/gateway/v1/micro-apps:
        get:
            tags:
                - GatewayService
            operationId: GatewayService_ListMicroApps
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MicroAppList'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - GatewayService
            operationId: GatewayService_RegisterMicroApp
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterMicroAppRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MicroApp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/gateway/v1/services:
        get:
            tags:
                - GatewayService
            operationId: GatewayService_ListServices
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ServiceList'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - GatewayService
            operationId: GatewayService_RegisterService
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterServiceRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Service'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/gateway/v1/services/{id}/routes:
        get:
            tags:
                - GatewayService
            operationId: GatewayService_ListRoutes
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RouteList'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        MicroApp:
            type: object
            properties:
                name:
                    type: string
                route:
                    type: string
                bundleUrl:
                    type: string
                menuLabelKey:
                    type: string
                requirePermission:
                    type: string
        MicroAppList:
            type: object
            properties:
                microApps:
                    type: array
                    items:
                        $ref: '#/components/schemas/MicroApp'
        RegisterMicroAppRequest:
            type: object
            properties:
                serviceName:
                    type: string
                microApp:
                    $ref: '#/components/schemas/MicroApp'
        RegisterServiceRequest:
            type: object
            properties:
                name:
                    type: string
                grpcHost:
                    type: string
                restPrefix:
                    type: string
        Route:
            type: object
            properties:
                path:
                    type: string
                method:
                    type: string
        RouteList:
            type: object
            properties:
                routes:
                    type: array
                    items:
                        $ref: '#/components/schemas/Route'
        Service:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                grpcHost:
                    type: string
                restPrefix:
                    type: string
                routes:
                    type: array
                    items:
                        $ref: '#/components/schemas/Route'
                microApp:
                    $ref: '#/components/schemas/MicroApp'
        ServiceList:
            type: object
            properties:
                services:
                    type: array
                    items:
                        $ref: '#/components/schemas/Service'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: GatewayService
